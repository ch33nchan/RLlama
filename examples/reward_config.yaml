frozen_lake_goal:
  name: frozen_lake_goal
  initial_weight: 1.0
  decay_schedule: 'none'
frozen_lake_hole:
  name: frozen_lake_hole
  initial_weight: 1.0
  decay_schedule: 'none'
step_penalty:
  name: step_penalty
  initial_weight: 1.0 # Was 0.5 with decay, now constant 1.0
  decay_schedule: 'none'
  # Example of adding decay back if needed:
  # initial_weight: 0.5
  # decay_schedule: 'linear'
  # decay_steps: 5000
  # min_weight: 0.0